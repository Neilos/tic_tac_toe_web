<!DOCTYPE html>
<html>
  <head>
    <link type="text/css" href="styles.css" rel="stylesheet">
    <script src='http://code.jquery.com/jquery-latest.min.js'></script>
    <script src="/javascripts/main.js"></script>
  </head>
  <body>
    <div class=container>
      <h2>Tic Tac Toe</h2>
      <button type="button"><a href="/newgame">New Game</a></button>
      <div id="game_instructions">
        <br/>
      </div>
      <br/>
      <table id="board">
        <tr>
        <% @squares.each_with_index do |square, number| %>
          <%= "</tr><tr>" if number % 3 ==0 && number > 0 %>
          <td <%="id='square#{number}'"%> ><%= square %></td>
        <% end %>
        </tr>
      </table>
    </div>
  </body>
</html>